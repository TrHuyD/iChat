@model List<string>

<h2>User's Servers</h2>

@if (Model.Count == 0)
{
    <p>User is not a member of any servers</p>
}
else
{
    <ul>
        @foreach (var server in Model)
        {
            <li>@server</li>
        }
    </ul>
}
