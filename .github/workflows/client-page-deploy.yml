name: Build and Deploy iChat.Client to GitHub Pages

on:
  push:
    branches:
      ["Action-testing"]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'
      
	- name: Publish Blazor app
	  run: |
	    dotnet publish ./iChat.Client -c Release -o ./publish -p:BaseHref="/" -p:PublishTrimmed=false

          # Ensure critical files exist
          touch ./publish/wwwroot/.nojekyll
          touch ./publish/wwwroot/index.html
      
      - name: Verify publish output
        run: |
          echo "Contents of wwwroot:"
          ls -la ./publish/wwwroot
          echo "Checking framework files:"
          ls ./publish/wwwroot/_framework/
          echo "Checking for index.html:"
          cat ./publish/wwwroot/index.html
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './publish/wwwroot'
      
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4