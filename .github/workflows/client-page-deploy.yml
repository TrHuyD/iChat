name: Build and Deploy iChat.Client to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'
      
      - name: Publish Blazor app
        run: |
          dotnet publish ./iChat.Client -c Release -o ./publish -p:BaseHref="/iChat/" -p:PublishTrimmed=false
          # Create .nojekyll file in wwwroot
          touch ./publish/wwwroot/.nojekyll
      
      - name: Verify publish output
        run: |
          ls -la ./publish/wwwroot
          echo "Checking for important files:"
          ls ./publish/wwwroot/_framework/
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './publish/wwwroot'
      
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4